"use client";

/**
 * Chart — Recharts-powered price chart with Fibonacci overlay.
 * Placeholder: will render retracement lines on a candlestick / area chart.
 */
export function Chart() {
  return (
    <div className="flex items-center justify-center rounded-xl border border-slate-800 bg-slate-900 p-8">
      <span className="text-sm text-slate-400">Chart — coming soon</span>
    </div>
  );
}
